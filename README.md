# ProxyPool

A proxy pool system with monitoring and a dashboard.

## Phase 1 Instructions
1. We have added `.gitignore` and `README.md`. You should run `git init` and commit this state.
2. The SQLite migration fallback is now mapped to `DB_TYPE=sqlite` if you want to test without MySQL.
